package AbstractAndInterface;

public class PDMExercise2 {
    /*
    class Person{}
    class Student extends Person{}

    [1] Student s1 = new Student();
    : 올바른 객체 생성 방법이다.

    [2] Person s2 = new Student();
    : 하위클래스를 상위클래스 형식으로 생성하는 업캐스팅으로 올바른 방법이다.

    [3] Person p1 = new Person();
    : 올바른 객체 생성 방법이다.

    [4] Student s1 = new Person();
    : 올바르지 않은 방법이다. 상위 클래스를 하위 클래스로 다운캐스팅을 하기 위해서는
      하위 클래스의 어떤 형식으로 다운 캐스팅을 할지 타입을 명시적으로 지정해줘야 한다.
      ex) Person person = new Student();
          Student s1 = (Student)person;

      그렇다면 아래와 같은 코드는 정상적으로 작동할까?
      Student s1 = (Student)new Person();

      컴파일 오류는 발생하지 않지만,런타임오류가 발생한다.
      그렇기 때문에 다운캐스팅은 이미 생성한 객체에만 사용해야 한다.

     */
}
